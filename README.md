# 🌤️ Weather Application - Приложение погоды

Простое консольное приложение для получения текущей погоды с кэшированием и автоматическими повторами запросов.

## 🚀 Возможности

* **Погода по городу** - получение данных о погоде по названию города
* **Погода по координатам** - получение данных по широте и долготе
* **Кэширование** - сохранение последнего успешного ответа в `weather_cache.json`
* **Автоматические повторы** - до 3 попыток при ошибках с экспоненциальной паузой (1s, 2s, 4s)
* **Резервные данные** - использование кэша при сетевых ошибках (если данным < 3 часов)
* **Консольное меню** - простой и понятный интерфейс

## 📋 Требования

* Python 3.6+
* Библиотеки: `requests`, `python-dotenv`
* API ключ от OpenWeatherMap
* Интернет-соединение для получения данных

## 🛠 Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/alexklychnikov-ui/CurrencyAPI
cd API
```

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` с API ключом:

```env
API_KEY=ваш_ключ_openweathermap
```

🔗 Получить API ключ: https://openweathermap.org/api

4. Запустите приложение:

```bash
python weather_app.py
```

## 📖 Использование

### Консольное меню

Запустите `python weather_app.py` для открытия главного меню:

```
🌤️  Приложение погоды
==================================================
1. Погода по названию города
2. Погода по координатам
0. Выход
```

### Примеры использования

**Пример 1: Погода в Москве**

```
Выберите опцию (0-2): 1
Введите название города: Moscow
Получаем погоду для города: Moscow
Погода в Moscow: 5°C, облачно с прояснениями
```

**Пример 2: Погода по координатам**

```
Выберите опцию (0-2): 2
Введите широту: 55.7558
Введите долготу: 37.6173
Получаем погоду для координат: 55.7558, 37.6173
Погода в Moscow: 5°C, облачно с прояснениями
```

## 🏗 Структура проекта

```
API/
├── weather_app.py      # Главный модуль приложения погоды
├── http_client.py      # HTTP клиент с ретраями
├── country_info.py     # Модуль информации о странах
├── weather_cache.json  # Кэш данных о погоде
├── requirements.txt    # Зависимости проекта
├── .env               # API ключи (не в репозитории)
├── .env_example       # Пример файла с ключами
├── .gitignore         # Игнорируемые файлы
└── README.md          # Документация
```

### Описание модулей

#### `weather_app.py`

Главный модуль приложения с функциями:

* `get_current_weather(city, latitude, longitude)` - получение текущей погоды
* `get_weather_by_city(city)` - погода по названию города
* `get_weather_by_coordinates(latitude, longitude)` - погода по координатам
* `get_coordinates(city)` - получение координат города
* `save_to_cache(data)` - сохранение данных в кэш
* `load_from_cache()` - загрузка данных из кэша
* `print_weather_info(weather_data)` - форматированный вывод погоды

#### `http_client.py`

HTTP клиент с поддержкой ретраев:

* `get_with_retries(url, params, headers, timeout, retries)` - GET запрос с повторами
* `get(url, params, headers, timeout)` - стандартный GET запрос
* `post(url, data, json_data, headers, timeout)` - POST запрос
* Обработка ошибок 429 (Too Many Requests) и 5xx

## 🔄 Кэширование и ретраи

### Кэширование

Приложение автоматически сохраняет успешные ответы в `weather_cache.json` с метаданными:

```json
{
  "name": "Moscow",
  "main": {"temp": 5},
  "weather": [{"description": "облачно"}],
  "fetched_at": "2025-12-02T15:30:00"
}
```

* **Кэш свежий** (< 3 часов) → используется при сетевых ошибках
* **Кэш устарел** (> 3 часов) → игнорируется
* **Кэш отсутствует** → создаётся после первого успешного запроса

### Ретраи и троттлинг

При ошибках 429 или временных сетевых сбоях:

* **1-я попытка** → немедленно
* **2-я попытка** → пауза 1 секунда
* **3-я попытка** → пауза 2 секунды
* **4-я попытка** → пауза 4 секунды

Экспоненциальная задержка: `2^attempt` секунд

## 🌐 Источник данных

Данные о погоде получаются с **OpenWeatherMap API**:

* Текущая погода в реальном времени
* Поддержка 200,000+ городов
* Данные на русском языке
* Метрическая система измерений

API endpoints:
* `https://api.openweathermap.org/data/2.5/weather` - текущая погода
* `https://api.openweathermap.org/geo/1.0/direct` - геокодирование

## 💡 Принципы архитектуры

Проект следует принципам **разделения ответственности** и **MVP**:

* **API слой** (`http_client.py`) - HTTP запросы и обработка ошибок
* **Бизнес-логика** (`weather_app.py`) - получение и обработка погоды
* **Кэширование** - встроенное в основной модуль
* **Интерфейс** - консольное меню в `main()`

## 🔧 Расширение функциональности

### Изменение времени жизни кэша

В `weather_app.py` измените параметр:

```python
if datetime.now() - fetched_time < timedelta(hours=6):  # было 3
```

### Изменение количества ретраев

В `http_client.py` измените параметр:

```python
def get_with_retries(..., retries: int = 5):  # было 3
```

### Добавление новых источников погоды

Создайте новую функцию в `weather_app.py`:

```python
def get_weather_from_alternative_api(city: str) -> dict:
    # ваша реализация
    pass
```

## 📝 Обработка ошибок

Приложение корректно обрабатывает:

* ❌ Отсутствие API ключа
* ❌ Неверное название города
* ❌ Неверный формат координат
* ❌ Сетевые ошибки (с использованием кэша)
* ❌ Ошибки 429 Too Many Requests (с ретраями)
* ❌ Временные сбои сервера 5xx (с ретраями)

## 📦 Зависимости

```txt
requests>=2.31.0
python-dotenv>=1.0.0
```

## 🖥 Системные требования

* Python 3.6+
* Windows 10 Pro (тестировалось)
* Интернет соединение для API запросов

## 📝 Лицензия

Проект создан в учебных целях. Свободное использование.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Внесите изменения и закоммитьте (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории с описанием:
* Ошибки и текст сообщения
* Шаги для воспроизведения
* Версия Python и ОС

## 🔗 Ссылки

* [OpenWeatherMap API Documentation](https://openweathermap.org/api)
* [Requests Library](https://requests.readthedocs.io/)
* [Python-dotenv](https://pypi.org/project/python-dotenv/)

---

**Версия:** 1.0.0  
**Дата:** Декабрь 2025  
**Автор:** alexklychnikov-ui
